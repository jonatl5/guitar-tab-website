# Minimal requirements for deployment
# Only essential packages needed to run the application

# Web framework
fastapi==0.119.0
uvicorn[standard]==0.37.0
python-multipart==0.0.20

# Image processing
# Use the headless build for server/container environments.
opencv-python-headless==4.11.0.86
pillow==11.3.0
numpy==1.26.4

# ML models
ultralytics==8.3.214
torch==2.2.2
yt-dlp
# Note: ffmpeg must be installed on the system for yt-dlp MP4 merge.

# Core dependencies
pydantic==2.12.2
starlette==0.48.0
